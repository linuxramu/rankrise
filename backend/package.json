{
  "name": "rankrise-backend",
  "version": "1.0.0",
  "description": "RankRise Backend API Worker",
  "main": "worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "itty-router": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.57.0"
  }
}
